'use client';

// Superadmin main page - shows admin dashboard by default
import AdminDashboard from '@/app/admin/page';

export default function SuperAdminDashboard() {
  // Show admin dashboard by default
  return <AdminDashboard />;
}
